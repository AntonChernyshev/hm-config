# This configuration file for Mosquitto allows anonymous connections.
# This is suitable for a closed simulation environment but should not
# be used in a production system without proper authentication.

# The port the broker will listen on.
listener 1883

# Allow clients to connect without a username or password.
allow_anonymous true
